version: '3'
services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app
    command: ["yarn", "start"]
    ports:
      - "3000:3000"
    stdin_open: true
    environment: 
      CHOKIDAR_USEPOLLING: "true"
